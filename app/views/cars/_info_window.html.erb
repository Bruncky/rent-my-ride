<div class="card">
  <%= cl_image_tag car.photo.key, class: "card-img-top" %>
  <div class="card-body">
    <p class="card-text">
      <%= car.model %>
      <%= car.location %>
      <strong><%= car.price %></strong> € per day
      <%= link_to "", car_path(car), class: "fas fa-key", style: "text-decoration: none; color: black"%>
    </p>
  </div>
</div>
