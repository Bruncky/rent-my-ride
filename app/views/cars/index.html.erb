<h1>Cars Index</h1>

<ul style="list-style: none">
  <% @cars.each do |car| %>
    <li><%= image_tag car.attached_pics, width: 400, height: 300, crop: :fill %></li>
    <li><%= car.model %></li>
    <li><%= link_to "", car_path(car), class: "fas fa-info-circle" %></li>
  <% end %>
</ul>
